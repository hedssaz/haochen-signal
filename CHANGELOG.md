# 更新日志

本文记录“浩宸信号”的重要变更。

## [未发布]

### 新增

- 新增无模型启动与动态供应商客户端：缺少配置时直接原子创建空 v2 配置，不再首屏强制询问供应商、模型或 API Key；未绑定模型的普通问题和 `/compact` 在注册前台任务、写会话或发起网络请求前给出明确提示，本地斜杠命令仍可使用。每轮任务与压缩重新解析当前模型、供应商和专属凭据，按供应商请求配置与凭据身份复用客户端；切换后下一轮立即使用新的客户端、`modelId` 和上下文上限，红眼审查复用当前模型。迁移供应商继续兼容旧全局环境变量与 Keychain 条目。
- 新增独立 `/model` 模型配置界面：普通输入与命令建议在面板内隐藏，模型按供应商分组并支持方向键切换、添加、编辑、删除和 Esc 返回；添加向导提供隐藏 Key、`GET /models` 选择与手动 Model ID，两种路径都进入默认显示名和 128000 最大上下文的详情页。保存与发现通过可注入控制器接线，配置 JSON 不含 Key；macOS 按供应商写入 Keychain，其他平台仅保留当前进程凭据，切换后立即更新最大上下文并按模型隔离最近 usage。
- 新增真实模型用量底栏：代理在验证唯一合法 `finish` 后、工具执行前发布 `inputTokens + outputTokens`，常驻显示已用/最大上下文；工具、审查、确认与上传阶段显示不重复累计的上一轮总量，缺失 usage 时明确标为未知，同时保留原有每个非空流分片计 1 的 `↓ tokens` 约定。
- 新增供应商专属凭据与模型发现：环境变量与 macOS Keychain 服务名共用稳定供应商 ID 的 UTF-16 code unit 固定四位十六进制后缀，避免原始 ID 进入服务名，并区分大小写、标点、非 ASCII 与未配对代理项；仅当 `legacy-provider` 同时引用 `legacy` 凭据时显式兼容旧服务项，Linux/Windows 使用隐藏临时输入。OpenAI-compatible `GET /models` 支持取消、超时、2 MiB 响应上限、严格 `data[].id` 校验、稳定去重排序；所有内部错误按不可变 code 重建固定消息，外部传输与响应体错误统一脱敏，恶意 getter、`toString` 或 Proxy 原型 trap 都使用不依赖 `instanceof` 的 total 安全边界。
- 新增仅创建的安全 `write_file` 工具：复用结构化补丁的同目录临时文件、同步与原子发布链路，拒绝覆盖、越界和符号链接路径；确定性边界按规范化路径及内容 SHA-256 绑定审查范围，终端摘要与审计输入不保存文件正文。
- 新增多模型配置、真实上下文用量和安全 `write_file` 的可执行实施计划。
- 新增多供应商模型配置、独立 `/model` 管理界面、`GET /models` 模型选择、真实上下文/上一轮总 token 底栏，以及创建专用安全 `write_file` 的中文设计规格。
- 新增终端内存推理转录与 `/compact` 实时进度回调：模型轮次结束、工具轮次切换或异常清理时，非空推理会定稿为 `思考 ›` 条目；压缩摘要的每个非空流增量会在 Promise 完成前更新底部计数，结束时按精确总数对齐且不重复累加。
- 新增模型流的终端分区：`思考 ›` 只显示供应商 `reasoning_content`，`浩宸 ›` 只显示 `content`；底部按每个非空推理/回答增量计 1 个 token，首个回答增量立即切为“思考完成 · 正在回答”，避免把供应商用量或真实 tokenizer 分词误当成界面计数。
- 新增 `/compact` 的唯一前台操作、输入锁定与流式摘要计数，并将其 `AbortSignal` 原样传到模型流；压缩可被第一次 `Ctrl+C` 单独中止且不会写入代理任务中断记录。
- 新增 `read_file` 的 Unicode 码点字符分页：超长单行会返回 `nextCharacter`，可在保持相同行范围时以 `startCharacter` 续读。
- 新增 `web_search.limit` 的端到端规范化协议：可省略时固定为 10，显式值限制为 1 至 10，并纳入模型工具 Schema、边界范围和审批指纹。
- 新增可续读超长单行文件、统一联网搜索参数、锁定上下文压缩、分离思考/回答流和底部 token 状态的中文设计规格与实施计划。
- README 首屏纯恶搞警告，以及 macOS、Linux 和 Windows PowerShell 的源码安装与凭据说明。
- 跨平台可靠解析用户主目录，并按平台提供首次 API Key 隐藏输入与 macOS Keychain 保存提示。
- 三平台 GitHub Actions 验证矩阵，覆盖 Node 20 的测试、类型检查、构建和版本入口。
- 新增终端语义分区与持续运行状态：用户、浩宸、工具、审批、结果、错误分别使用独立标题和颜色；工具调用显示脱敏缩略参数，任务活动期间持续显示当前阶段和中止键。
- 新增完整执行门可见性：每次工具调用明确显示确定性边界直接放行或拒绝、AI 自动审查结论、人工确认选择、会话许可命中及范围漂移结果，不再用模糊的统一前缀表示审批。
- 新增按工作区哈希归类的交互式 `/resume` 选择器，显示会话时间与首句预览，支持方向键、回车和 Esc；旧版会话保留在“工作区未知”分组。
- 新增 Ink 交互式信号场终端及真实启动入口：提供稳定的代理事件 reducer、首次 API 配置、全部本地斜杠命令、真实失败摘要与双阶段 Ctrl+C 中止；启动时创建模型、会话、审计、工具执行门与主代理循环，临时 API Key 不回显、不写配置，也可选择保存至系统钥匙串。
- 新增狼王主代理循环：构建带不可信内容边界的系统提示，向模型公开已注册工具，合并流式工具调用分片并顺序执行，将真实结果写入会话并送回模型；轮次、工具次数、非法 JSON 单次修正和全阶段中止均有明确边界。
- 新增标准版与超大单文件版双构建：标准入口外置运行依赖，单文件入口完整打包运行依赖；两者均保留非压缩源码结构、唯一 shebang 和 `0755` 权限。
- 新增发布集成测试与本地 OpenAI-compatible 流式模拟服务器，覆盖分片工具调用、顺序响应、双构建行为一致性及 npm 发布文件精确白名单。
- 新增 `test:integration` 与发布前自动构建命令，并补全安装、配置、交互命令、权限审查、日志隐私、构建和发布中文说明。
- 新增无工具权限的红眼 AI 审查器与统一工具执行门，严格校验单一 JSON 审查对象，失败时转人工确认，并以执行前重分类、内存会话指纹许可和全链路脱敏审计阻止范围漂移及工具绕过。
- 新增确定性边界守卫，按固定优先级分类文件、命令、Git 与网络操作，使用真实路径和公开 URL 校验阻断越界、符号链接及编码绕过，并以规范化范围的稳定 SHA-256 指纹绑定单次批准。
- 新增确定性上下文选择与信号压缩：按任务优先级在固定 UTF-8 token 预算内装载上下文，单文件保留首尾截断，并用 Zod 校验结构化历史摘要且在失败时保留原事件。
- 新增安全联网检索工具：DuckDuckGo HTML 搜索与 Readability 正文提取均支持取消、15 秒超时、2 MiB 响应上限、最多三次重定向及不可信外部正文标记；联网前及每次重定向后会校验 HTTP(S) URL、DNS 全部解析结果和私有网络地址。
- 新增可取消的前台命令工具与只读 Git 工具，支持工作区 cwd 边界、超时进程组清理、按字节截断及完整临时日志，并固定 status、diff、log 参数族。
- 新增基于真实路径的工作区边界与安全文件工具，支持稳定文件列表、限额文本搜索、行范围读取，以及带全量预验证和 SHA-256 复核的结构化补丁。
- 新增 OpenAI-compatible Chat Completions 流式客户端，支持跨块 SSE、文字与工具调用聚合、用量统计、可取消重试、认证脱敏及脚本化模型测试响应。
- 新增递归凭据脱敏、追加式 JSONL 会话与审计记录，支持截断恢复、损坏行定位、会话排序及匿名工作区标识。
- 新增配置结构校验、XDG 标准目录解析与权限为 `0600` 的原子配置保存。
- 新增 API Key 的环境变量、macOS 钥匙串与临时输入优先级，以及可注入进程执行器的钥匙串适配。
- 建立工程基础，提供产品元数据与 `haochen` CLI 可执行入口。
- 确立“浩宸信号 / Haochen Signal”的产品名称、包名与全局命令。
- 完成交互式 AI 编程 CLI 的整体设计规格。
- 定义主代理循环、OpenAI-compatible 模型适配、狼群节点工具箱、边界守卫、红眼审查和王冠记录。
- 定义正常发布版本与超大单文件版本两种构建产物。
- 建立中文 README、CHANGELOG 与 Git 项目基础。
- 完成覆盖工程基础、模型协议、工具、安全审查、会话、终端界面和双构建的实施计划。
- 为后续功能开发配置隔离工作树目录。
- 配置本地开发进度账本目录，支持多任务中断恢复。
- 修正实施计划中端到端模拟响应数量的表述。

### 修复

- 修复模型配置面板吞掉 `Ctrl+C`、保存无法取消、并发保存乱序及提交点误判，并补齐现有供应商追加第二个模型、供应商环境变量显示和删除模型 usage 清理：配置写入在临时文件重命名前检查取消并清理未发布文件，`rename` 调用开始后则以其成功返回作为已提交，不再因同一 signal 随后中止而拒绝运行时和界面更新；保存请求串行化且仅当前代际可更新运行时与界面，因此无后续 B 时 disk/runtime/UI 接纳已提交 A，有 B 时最终三者均为 B。发现与保存继续共用可查询状态的可中止控制器并保持两阶段退出语义；`A` 可选择当前或新供应商，当前供应商复用 provider、API 地址与已有凭据；动作页显示专属环境变量，保存后淘汰已删除模型的上下文用量。
- 修复 `write_file` 在硬链接发布后用 `lstat + unlink` 回滚所留下的最终 TOCTOU 窗口：硬链接调用现在是不可逆提交点，调用前保留最后一次中止检查，开始后不再删除目标；代理循环会等待 `write_file` 与 `apply_patch` 返回，先显示并持久化真实工具结果，再记录任务中断，且不会进入下一轮模型请求。
- 修复任务等待供应商凭据时尚未绑定中断 writer、任务完成后又未清理 writer 的会话串线问题：每个任务在解析凭据前固定 taskSessionId 与幂等中断写入器，`finally` 只清除自己的绑定，等待 Key 时的第二次 `Ctrl+C`、后续清空或恢复都写入正确会话。
- 修复真实 CLI 入口沿用 Ink 默认 `exitOnCtrlC`、导致框架先于 App 退出且两阶段中止逻辑收不到按键的问题：入口现把 Ctrl+C 统一交给 App；动态凭据提示等待期间第一次按键只取消任务、清空 broker 并恢复命令输入，空闲或仍在收尾时的后续按键继续遵循既有退出规则。
- 修复动态供应商凭据提示与 Ink 同时消费 stdin 的问题：运行中的 CLI 不再创建额外 readline 或在提交 Key 后调用 `setRawMode(false)`，而是通过可取消的凭据 broker 把供应商元数据交给 App，在 Ink 内用独立圆点掩码状态接收临时 Key；Key 不进入终端转录、配置、会话或审计，提交后 raw 输入保持可用，下一条本地命令一次 Enter 即可执行。
- 修复模型配置中 `models` 数组交错排列多个供应商时，界面按供应商分组显示、方向键却按原数组跳转，导致光标与 Enter 实际切换模型不一致的问题；状态机和视图现共用稳定的供应商分组扁平顺序。
- 修复模型持续输出思考时仍会在固定 60 秒总时长到达后被强制取消、导致编码任务尚未调用写入工具就超时的问题：`timeoutMs` 现表示无流量超时，每个响应流分片都会重新计时；主代理提示同时要求决定需要工具后立即行动，禁止重复规划同一方案。
- 修复 DNS-over-HTTPS 返回非成功状态时未取消响应体、共享 dispatcher 可能等待未读流直到总超时的问题；失败响应现先在同一请求期限内取消，再关闭传输。
- 修复启用 Fake-IP 模式的本机网络代理把 DuckDuckGo 搜索端点映射到 `198.18.0.0/15`，导致 `web_search` 被 SSRF 边界误判为非公网地址的问题：仅在全部本地 DNS 答案都属于该代理专用范围时，使用固定公网 DNS-over-HTTPS 重新解析，并继续校验及固定最终公网连接；普通私网答案不会触发回退。
- 修复 `/compact` 发出取消后若底层 Promise 长时间收尾，后续 `Ctrl+C` 永远无法退出的问题：第一次按键记录压缩中止请求，操作仍活动时第二次按键保存并退出，且两次都不会伪造代理任务中断事件；同时把 `web_search.query` 的模型 JSON Schema 改为精确表达 trim 后 1..500 的 ECMA-262 pattern，500 字正文带首尾空白可通过，纯空白与 501 字正文会被真实 Schema 校验器拒绝。
- 修复纯推理工具轮次在 `assistant_turn_finished` 后从终端消失、压缩追加期间取消被误报为失败、`web_search.query` 长度契约只在工具实现中生效，以及 `/compact` 计数只能在结束后跳变的问题：推理现在仅定稿到内存界面；摘要追加开始即成为不可逆提交点；查询在去空白后统一限制为 1 至 500；压缩进度实时显示并在未提交中止时拒绝伪成功和伪最终计数。
- 修复推理流和回答流此前都缺少独立终端语义的问题：供应商的 `reasoning_content` 曾未进入专用实时分区，`content` 首个非空增量也无法立即标记回答阶段；现按两种字段分区并逐增量计数。
- 修复 `/compact` 未登记为与代理任务互斥的前台操作、无法接收界面中止信号的问题：压缩期间不再接受新输入，第一次 `Ctrl+C` 仅取消压缩而不触发代理退出或中断持久化。
- 修复超长单行只有行号分页、达到单页上限后无法定位续读位置的问题：读取结果现在返回 `nextCharacter`，续读按 Unicode 码点偏移且保持既有行范围。
- 修复 `web_search` 虽接受 `limit` 但确定性边界把它当作未知字段的问题：模型 Schema、输入校验、规范化范围、审批指纹和执行值现统一为默认 10、范围 1 至 10 的同一参数。
- 修复慢速 Windows CI 上中止回归测试在工具真正启动前抢先取消造成的偶发失败：测试现等待工具进入执行阶段后再触发取消，仍验证忽略信号的工具不会阻塞主代理退出。
- 修复 Windows 裸命令解析与边界审批身份不一致的问题：确定性候选解析不再隐式优先工作目录，审批指纹绑定可信可执行文件绝对身份，执行前复核后仅启动同一身份；同时让已有配置但无环境变量或 Keychain 凭据时继续通过隐藏输入读取仅供本次进程使用的 API Key，并补全三平台自动验证、凭据方式和英文免责声明契约。
- 修复首次 Windows 跨平台验证暴露的兼容性问题：命令工具改用安全解析 npm 等 Windows shim 的跨平台进程入口，工作区相对路径统一输出正斜杠；构建打包通过当前 Node 进程调用 npm 入口，并将 POSIX 权限位、信号与进程组断言和 Windows 专属进程树断言按真实平台语义隔离。
- 修复普通寒暄会擅自扫描整个工作区的问题：纯问候强制关闭工具调用，系统提示明确禁止为了“了解项目”主动枚举文件；递归文件列表最多返回 500 项并标记截断，避免大型目录长期占住任务。
- 修复任务运行期间仍显示可输入提示、提交后才报“当前任务仍在运行”的问题：执行期间输入区域现明确锁定，只接受 `Ctrl+C` 中止，任务结束后自动恢复。
- 修复实时阶段同时写入会话记录造成 `状态 › 状态` 重复标题的问题：瞬时状态只保留在底部运行指示器中。
- 修复主界面缺少完整终端交互反馈的问题：进入信号场前清空可见终端与回滚缓冲，输入 `/` 时显示可筛选的本地命令面板并支持 `Tab` 补全，提交后的用户输入以 `浩宸 ›` 条目保留在会话记录中。
- 修复首次配置在显示隐藏的 `API Key` 提示后直接退出的问题：切换到隐藏输入时会恢复已暂停的终端输入流，并以真实 TTY 回归测试确保流程继续进入钥匙串保存询问和主界面。
- 将本地交付产物目录 `outputs/` 纳入 Git 忽略规则，保持发布打包后的主分支工作区干净。
- 修复在主仓库运行测试时重复扫描隔离工作树的问题：Vitest 现在明确排除 `.worktrees` 与 `worktrees` 目录。
- 修复 `/permissions` 使用启动时会话许可数量快照的问题：入口现将与工具执行门共享的许可集合直接传给终端，用户选择“本会话允许”后可立即看到最新许可数量。
- 修复交互终端真实接线：红眼或边界确认请求现会显示工具、风险和规范化范围，可选择仅本次允许、会话允许或拒绝；非交互终端明确拒绝。第二次 Ctrl+C 会先持久化唯一中断事件，退出与清空均追加会话检查点；带工具调用的中间文本不再误标为任务完成。首次配置仅接受无用户凭据的 HTTP(S) API 地址。
- 收紧狼王主代理循环协议：只有 `stop` 可完成纯文本、只有 `tool_calls` 可进入工具阶段；整批工具调用会先校验非空唯一 ID 与合法 JSON，按模型响应轮次提供一次修正机会。会话追加一旦开始便串行等待完成，中止记录始终排在此前已开始的写入之后，避免后台追加覆盖最终中断状态。
- 修复 Node.js 20 发布兼容性：Ink 固定为支持 Node.js 20 的 6.8 系列，并显式安装单文件打包所需的 `react-devtools-core` 可选运行依赖。
- 修复 ESM 单文件执行 CommonJS 依赖时无法动态加载 Node 内置模块的问题：构建头部通过 `createRequire(import.meta.url)` 提供兼容层。
- 修复 curl `--connect-to` 与 `--resolve` 对非标准 IPv4 字面量的私网绕过：统一按 WHATWG URL 规范化十六进制、八进制、十进制和缩写 IPv4 后，再判定本机、内网及保留地址。
- 完成确定性边界守卫批准阻断修复：原始包装链中的紧凑 `env -S/-C`、curl `@file` 上传及 `--resolve/--connect-to` 重映射会先执行真实路径与非公开目标拒绝；通用 `run_command` 中的 Git、env、curl 一律需要用户确认，仅专用只读 Git 工具可直接放行。
- 收紧红眼执行门：审查风险升高、影响范围扩大或出现无法执行的约束时强制转人工确认，审查模型改由调用方显式指定；会话许可继续绑定工作区指纹，CLI 分离、换行及引号拼接凭据参数统一脱敏；执行前审计不可用会阻止工具，执行后审计失败则保留真实结果并附加警告。
- 继续收紧确定性边界守卫：`env --` 递归展开实际命令，`env -S` 保守按 Shell 语义确认并保留明确私网重映射的拒绝；补全 curl `--connect-to` 私网映射校验，且仅明确只读的 Git 子命令可降为审查，其余 Git 子命令均需确认。
- 收紧确定性边界守卫的保守策略：所有 Shell 解释调用至少需要确认，紧凑 Git 路径和 Shell 命令文本仍执行工作区边界拒绝；补全 `git clean --force`、curl 编码上传、短选项组合及私网 `--resolve` 校验，并严格拒绝补丁操作的不可枚举或符号字段。
- 加固确定性边界守卫：Shell 引号与转义不再绕过路径及高危命令优先级，拒绝 curl URL glob 私网目标，识别紧凑上传参数与 `git send-pack`，扩展凭据路径，使用真实工作区绑定指纹，并拒绝带访问器或额外字段的补丁数组。
- 修复信号上下文压缩后的读取投影：摘要事件记录其覆盖的 JSONL 前缀，多次压缩只保留最新有效摘要与真实最近事件；超大当前任务与相关文件会在剩余预算内二次截断，极小预算使用明确的任务降级标记且始终不超限。
- 加固联网 SSRF 边界：把每次 DNS 校验结果固定到该跳实际连接并保留原始 Host/TLS SNI，统一 DNS、重定向、请求、响应读取和资源清理总时限，仅放行 IANA 全局单播地址；即使响应体读取永久停滞，超时或调用方取消也会立即取消响应流并释放 dispatcher，且 2 MiB 边界语义保持一致。
- 修复命令输出日志删除失败被静默忽略的问题：关闭句柄后进行有界重试，仍无法删除时截断内容并收紧至 `0600`，同时返回不含路径的清理状态。
- 修复只读 Git diff 仍会触发本地 fsmonitor 的问题；Windows 命令清理现在会在首次 `taskkill` 前按 PID 与创建时间快照整棵后代树，检查每次退出码并验证所有原进程身份消失，无法证明时返回明确的清理失败。
- 加固命令与只读 Git 工具：Windows 使用固定 `taskkill` 进程树清理，Git 隔离继承环境与外部 helper，日志写失败不再泄漏残缺文件，并修正 UTF-8 码点截断、NUL 日志解析及截断日志路径传播。
- 修正文件补丁提交点语义：新增、更新或删除已经落盘后，临时文件清理、句柄关闭及发布确认错误改为脱敏警告，不再误报整项操作失败。
- 加固安全文件工具：拒绝二进制更新，使用同目录临时文件原子发布，统一脱敏任意异常与取消原因，并限制分块读取、搜索摘要及 CR/LF 行解析边界。
- 修复模型请求构造异常的凭据泄漏、底层取消承诺无限阻塞，以及超大 `Retry-After` 触发 Node 定时器溢出和提前重试的问题。
- 加固 Chat Completions 流式边界：配置超时覆盖请求与响应全周期，所有模型错误统一脱敏，异常终止会取消底层响应，并校验 finish 协议、混合 SSE 换行和 HTTP-date 重试时间。
- 修复命令参数及内联文本中的认证 Header 与 `api_key` 脱敏旁路，不再要求敏感字段位于行首。
- 泛化原始认证 Header 与私钥块脱敏，覆盖任意认证方案、Cookie 及标签包含 `PRIVATE KEY` 的完整或截断块。
- JSON 前缀扫描仅接受标准四种空白，并统一零字节会话的读取、列表与首次追加语义。
- 加固会话与审计边界：阻止 ID 与符号链接路径穿越，追加前确定性修复截断尾部，并用 Zod 按行校验会话事件。
- 扩展认证 Header、GitHub/Stripe 令牌、凭据 URL 与二进制数据脱敏，同时避免误伤普通字段名。
- 仅接受非空绝对 XDG 路径；空值、纯空白和相对路径均安全回退至用户目录下的标准位置。
- 配置保存现在可注入文件操作以验证原子写入顺序，并覆盖 rename 失败时的临时文件清理。
- 强化原子保存回归测试，确保 UUID 临时文件始终不同于目标配置，避免失败清理误删原配置。
